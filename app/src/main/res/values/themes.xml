<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose paints everything; this theme only has to hand it a window. -->
    <style name="Theme.ReCMF" parent="@android:style/Theme.DeviceDefault.Light.NoActionBar" />
</resources>
