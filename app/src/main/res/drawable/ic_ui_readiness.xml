<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <!--
        A dial with its needle up and to the right. A gauge rather than a heart or a chart:
        this card is the one reading on the screen that is a verdict rather than a
        measurement, and it should not be mistaken for either the pulse tile or the charts.
    -->
    <path
        android:strokeColor="@android:color/white"
        android:strokeWidth="2"
        android:strokeLineCap="round"
        android:pathData="M3.5,17.5a10,10 0 1,1 17,0" />
    <path
        android:strokeColor="@android:color/white"
        android:strokeWidth="2"
        android:strokeLineCap="round"
        android:pathData="M12,17.5L16.5,10.5" />
</vector>
